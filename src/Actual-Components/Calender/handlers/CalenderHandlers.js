export const handleClickedScheduled = (
  postContent,
  postType,
  schedulePostDate,
  schedulePostTime,
) => {
  console.log(`${postContent}-${postType}`);
  console.log(schedulePostDate, "📅");
  console.log(schedulePostTime, "❤");
  console.log("⭐⭐");
};
